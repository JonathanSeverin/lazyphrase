TODO main priority
* UI for showing phrase content when a listbox item is hovered over
* Implement sorting of phrases based on hotkey


UX improvements
* UI for adding/modifying/deleting reposition phrases. Folder structure


Small stuff:
* Add to main README that you need to install xclip for pyperclip to work?


Issues that are not prioritized
* If you change a phrase's title/content/hotkey and click on that phrase in the list view, the changes will not be saved
This is caused by a check when pressing in the listbox. If the phrase is the same as the previous clicked phrase, do not save. If I remove that
check it solves the problem, but then Im saving each phrase everytime I switch phrase (even if nothing was changed).
So would need a dirty flag to fix it in a more solid manner. Do not care as of now
